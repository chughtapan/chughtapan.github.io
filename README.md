# chughtapan.github.io

Redirect shim. The site moved to https://vibe-blogging.vercel.app (custom domain to follow); `redirect.js` preserves the path and the destination maps the old al-folio URLs. Change `TARGET` in `redirect.js` and the two `<meta>`/`<link>` URLs when the domain is live.
